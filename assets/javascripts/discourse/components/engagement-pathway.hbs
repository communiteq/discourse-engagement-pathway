 {{#if this.showHere}}
  <div class="engagement-pathway-wrapper">
    <div class="wrap engagement-pathway-banner">
      <div class="ep-wrapper">
        {{#if this.endlevel}}
          <div class="ep-info">
            <b>{{i18n "engagement_pathway.endlevel_message"}}</b>
            {{#each this.info as |i|}}
              <div class="infoline">
              {{{i.html}}}
              </div>
            {{/each}}
            {{#if this.showEmoji}}
              <img src="{{this.emojiSrc}}" width="20" height="20">
              <img src="{{this.emojiSrc}}" width="20" height="20">
              <img src="{{this.emojiSrc}}" width="20" height="20">
            {{/if}}
          </div>
          <div class="ep-links">
            <div class="ep-links-header">{{{this.linksHeader}}}</div>
            {{#each this.links as |link|}}
              <div>
              {{{link.html}}}
              </div>
            {{/each}}
          </div>
        {{else}}
          <div class="ep-content">
            {{{this.message}}}
          </div>
          <div class="ep-level-task">
            {{#each this.tasks as |t|}}
              <div class="task">
                <a href="{{t.url}}">
                  <h3 class={{t.status}}>
                    {{d-icon t.icon}}
                    {{t.text}}
                  </h3>
                </a>
              </div>
            {{/each}}
          </div>
        {{/if}}
      </div>
    </div>
  </div>
{{/if}}